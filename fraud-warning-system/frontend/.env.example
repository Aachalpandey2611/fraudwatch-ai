# Backend base URL for deployed frontend
# Local default in code is http://localhost:4000
VITE_API_URL=http://localhost:4000

# Socket server URL (usually same as backend URL)
VITE_SOCKET_URL=http://localhost:4000
